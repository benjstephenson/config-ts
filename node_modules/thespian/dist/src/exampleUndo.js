"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Thespian_1 = require("./Thespian");
const mismatched_1 = require("mismatched");
describe("Thespian By Example: Undo/Redo", () => {
    it("Add two commands and undo()", () => {
        const undoRedo = new UndoManager();
        const thespian = new Thespian_1.Thespian();
        const edit = thespian.mock("edit");
        const replace = thespian.mock("replace");
        // Given
        replace
            .setup(f => f.undo())
            .returnsVoid();
        edit
            .setup(f => f.details())
            .returns(() => "Edit");
        undoRedo.add(edit.object);
        undoRedo.add(replace.object);
        // When
        undoRedo.undo();
        // Then
        mismatched_1.assertThat(undoRedo.currentDetails()).is("Edit");
        thespian.verify();
    });
});
class UndoManager {
    constructor() {
        this.readyToUndo = [];
        this.readyToRedo = [];
    }
    add(command) {
        this.readyToUndo.push(command);
    }
    undo() {
        if (this.readyToUndo.length > 0) {
            const command = this.readyToUndo.pop();
            this.readyToRedo.push(command);
            command.undo();
        }
    }
    currentDetails() {
        if (this.readyToUndo.length > 0) {
            return this.readyToUndo[0].details();
        }
        return "None";
    }
}
//# sourceMappingURL=exampleUndo.js.map