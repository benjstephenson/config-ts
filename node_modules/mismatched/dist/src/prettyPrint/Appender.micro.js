"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Appender_1 = require("./Appender");
const assertThat_1 = require("../assertThat");
describe("Appender()", () => {
    const lineWidth = 20;
    let appender;
    beforeEach(() => {
        appender = new Appender_1.Appender(lineWidth);
    });
    it("initially", () => {
        assertThat_1.assertThat(appender.compose()).is("");
        assertThat_1.assertThat(appender.remaininglineWidth).is(lineWidth);
        assertThat_1.assertThat(appender.currentLineComplexity).is(0);
    });
    it("one line", () => {
        let text = "Hello";
        appender.add(text, 1);
        assertThat_1.assertThat(appender.remaininglineWidth).is(lineWidth - text.length);
        assertThat_1.assertThat(appender.currentLineComplexity).is(1);
        assertThat_1.assertThat(appender.compose()).is(text);
    });
    it("2 parts", () => {
        appender.add("Hello", 1);
        appender.add(" ");
        assertThat_1.assertThat(appender.remaininglineWidth).is(lineWidth - "Hello ".length);
        appender.add("World", 1);
        assertThat_1.assertThat(appender.remaininglineWidth).is(lineWidth - "Hello World".length);
        assertThat_1.assertThat(appender.currentLineComplexity).is(2);
        assertThat_1.assertThat(appender.compose()).is("Hello World");
    });
    it("2 parts at once", () => {
        appender.adds(["Hello", " ", "World"], 3);
        assertThat_1.assertThat(appender.remaininglineWidth).is(lineWidth - "Hello World".length);
        assertThat_1.assertThat(appender.currentLineComplexity).is(3);
        assertThat_1.assertThat(appender.compose()).is("Hello World");
    });
    it("2 lines", () => {
        appender.addsNewLine(["Hello"], 1);
        appender.add("World", 1);
        assertThat_1.assertThat(appender.currentLineComplexity).is(1);
        assertThat_1.assertThat(appender.compose()).is("Hello\nWorld");
    });
    it("2 lines with tabs", () => {
        appender.tabRight();
        assertThat_1.assertThat(appender.remaininglineWidth).is(lineWidth - 2);
        appender.addsNewLine(["Hello"]);
        assertThat_1.assertThat(appender.remaininglineWidth).is(lineWidth - 2);
        appender.add("World");
        assertThat_1.assertThat(appender.remaininglineWidth).is(lineWidth - "  World".length);
        assertThat_1.assertThat(appender.compose()).is("  Hello\n  World");
    });
    it("2 lines with tab in and then out", () => {
        appender.tabRight();
        appender.addsNewLine(["Hello"]);
        appender.tabLeft();
        assertThat_1.assertThat(appender.remaininglineWidth).is(lineWidth);
        appender.add("World");
        assertThat_1.assertThat(appender.compose()).is("  Hello\nWorld");
    });
});
//# sourceMappingURL=Appender.micro.js.map