"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const assertThat_1 = require("../assertThat");
const match_1 = require("../match");
describe("string:", () => {
    const actual = "abcdefghijkl";
    it("string.match:", () => {
        assertThat_1.assertThat(actual).is(match_1.match.string.match(actual));
        assertThat_1.assertThat(actual).is(actual);
    });
    it("string.startsWith:", () => {
        assertThat_1.assertThat(actual).is(match_1.match.string.startsWith("abc"));
    });
    it("string.endsWith:", () => {
        assertThat_1.assertThat(actual).is(match_1.match.string.endsWith("jkl"));
    });
    it("string.includes:", () => {
        assertThat_1.assertThat(actual).is(match_1.match.string.includes("abc"));
        assertThat_1.assertThat(actual).is(match_1.match.string.includes("cde"));
    });
});
//# sourceMappingURL=egMatchString.micro.js.map